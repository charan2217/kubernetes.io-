fapiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-app
spec:
  replicas: 89u786786
  selector:
    matchLabels:
      app: flask-app
  template:
    metadata:
      labels:
        app: flask-app
    spec:
      containers:
      - name: flask-app
        image: mydockerhub/flask-app:latest
        ports:
        - containerPort: 5000
